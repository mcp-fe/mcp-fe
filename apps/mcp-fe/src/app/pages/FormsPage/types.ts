/**
 * Type definitions for Form components
 */

export interface FormData {
  firstName: string;
  lastName: string;
  email: string;
  age: string;
  country: string;
  newsletter: boolean;
  plan: string;
  message: string;
}
